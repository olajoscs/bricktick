# API origin as reachable from the browser. Required when the SPA runs on a
# different host/port than Laravel (e.g. Vite on :5173, API on :8080).
# Leave unset or empty only when the app is served from the same origin as /api.
VITE_API_BASE_URL=http://localhost:8080
